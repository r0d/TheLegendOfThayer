import PyGame
import random

def move_player(x, y, keypress): 
	'''
	returns new (x, y) tuple based on the current x/y and the key that was pressed to trigger the event
	'''

def do_damage(x, y, direction): 
	'''
	does damage to monster in a square based on current (x,y) and the direction that the player is facing 
	''' 

def update_screen('''It's going to need a lot of information'''): 
	''' 
	update the screen based on everything that happened in the last tick
	'''

